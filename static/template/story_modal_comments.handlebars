<ul class="list-group">
  {{#each comments}}
    <li class="list-group-item">
      <p>
        <a href="{{this.user.html_url}}">
          <img class="avatar" src="{{this.user.avatar_url}}"><span>{{this.user.login}}</span>
        </a>
        <span class="comment-date">{{date this.updated_at}}</span>
        <a class="nolink" href="javascript:void(0)">
          <span id="{{this.id}}" class="glyphicon glyphicon-remove pull-right comment-delete"></span>
        </a>
      </p>
      <br>
      <p>{{this.body}}</p>
    </li>
  {{/each}}
  <li class="list-group-item">
    <form>
      <textarea id="newComment" class="form-control" placeholder="Leave a comment"></textarea>
      <p></p>
      <button  id="newCommentBtn" type="button" class="btn btn-success btn-sm">Comment</button>
    </form>
  </li>
</ul>